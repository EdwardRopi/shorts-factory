# Зависимости витрины на Render: без torch, scipy и numpy.
# Собирать ролики там нельзя — 512 МБ памяти на бесплатном тарифе не хватит
# ни на модель озвучки, ни на ffmpeg. Сайт показывает интерфейс и готовые ролики.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
python-dotenv>=1.0
